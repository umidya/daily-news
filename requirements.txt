# Exact pins — CI installs whatever is here on every run, so a floating
# ">=" floor means an upstream SDK release can break production with zero
# code change. Refresh deliberately: bump, run pytest, commit.
# Last refreshed: 2026-07-07 (all tests green on these versions).
anthropic==0.116.0
openai==2.44.0
feedparser==6.0.12
httpx==0.28.1
python-dateutil==2.9.0.post0
PyYAML==6.0.3
rapidfuzz==3.13.0
Jinja2==3.1.6
feedgen==1.0.0
python-dotenv==1.2.1
mutagen==1.47.0
