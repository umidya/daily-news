# Copy to app/.env and fill in. Variables prefixed EXPO_PUBLIC_ are baked into
# the build and visible in the app bundle, so don't put secrets here.

# Where the app fetches today.json + audio from. After GitHub Pages is set up,
# this will be: https://umidya.github.io/daily-news
EXPO_PUBLIC_DAILY_NEWS_BASE_URL=
